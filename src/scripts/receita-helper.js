export function initToggleIngredientes($dom = document) {
    // const $$itens = $dom.querySelectorAll('[data-bind="ingredientes"] .item');

    // $$itens.forEach(($item) => {
    //     $item.onclick = () => {
    //         $item.classList.toggle('feito');
    //     };
    // })
}